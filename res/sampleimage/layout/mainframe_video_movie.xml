<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout android:id="@layout/mainframe_video_movie" style="@style/fill_vertical"
  xmlns:android="http://schemas.android.com/apk/res/android">
    <LinearLayout android:layout_below="@id/titlebar" style="@style/fill_equal_width_vertical">
        <LinearLayout android:id="@id/no_list_item" style="@style/fill">
            <include layout="@layout/no_item_list" />
        </LinearLayout>
        <ListView android:scrollbarThumbVertical="@drawable/scrollbar_thumb" android:id="@id/list" android:nextFocusLeft="@id/list" android:nextFocusRight="@id/list" android:nextFocusUp="@id/list" android:nextFocusDown="@id/list" android:fadeScrollbars="false" style="@style/list_item_video" />
        <GridView android:scrollbarThumbVertical="@drawable/scrollbar_thumb" android:id="@id/grid" android:nextFocusLeft="@id/grid" android:nextFocusRight="@id/grid" android:nextFocusUp="@id/grid" android:nextFocusDown="@id/grid" android:fadeScrollbars="false" style="@style/grid_item" />
    </LinearLayout>
</RelativeLayout>