<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout android:id="@layout/mainframe_edit_playlist" style="@style/fill_vertical"
  xmlns:android="http://schemas.android.com/apk/res/android">
    <LinearLayout android:id="@id/subtitlebar" android:layout_below="@id/titlebar" style="@style/fill_width">
        <include layout="@layout/subtitlebar_edit_playlist" />
    </LinearLayout>
    <LinearLayout android:id="@id/list_edit_menu" android:background="@color/black" android:layout_below="@id/subtitlebar" style="@style/fill_width">
        <include layout="@layout/menu_edit_playlist" />
    </LinearLayout>
    <LinearLayout android:id="@id/list_edit_palylist" android:layout_below="@id/list_edit_menu" style="@style/fill">
        <com.nttdocomo.android.mediaplayer.view.listview.DndListView android:scrollbarThumbVertical="@drawable/scrollbar_thumb" android:id="@id/list" android:nextFocusLeft="@id/list" android:nextFocusRight="@id/list" android:nextFocusUp="@id/list" android:nextFocusDown="@id/list" android:fadeScrollbars="false" style="@style/edit_playlist_list_item" />
    </LinearLayout>
</RelativeLayout>